# FireLite local database encryption key (ChaCha20-Poly1305).
# REQUIRED - copy to `.env` and set the same key used to encrypt existing databases.
FIRELITE_ENCRYPTION_KEY=replace-with-your-32-hex-char-key